# Leave empty for local dev (Vite proxy forwards /api → http://localhost:3000)
# In production (Render), set this to your backend Render URL, e.g.:
# VITE_API_URL=https://deployment-xxxx.onrender.com
VITE_API_URL=https://deployment-bmrp.onrender.com
